@{
    ViewData["Title"] = "Admin Portal - Users Overview";
    Layout = "_AdminLayout";
}

@section Styles {
    <link rel="stylesheet" href="~/css/admin_css/users.css" asp-append-version="true" />
}

<div class="container-fluid py-4 bg-light">
    <div class="d-flex flex-column flex-md-row justify-content-between align-items-md-end mb-4 gap-3">
        <div>
            <div class="d-flex border-bottom overflow-auto text-nowrap custom-tabs">
                <button id="tab-active" onclick="switchUserTab('active')"
                        class="btn btn-sm fw-bold bg-dark text-white rounded-0 px-3 py-2 active-tab me-1">
                    Active Consumers
                </button>
                <button id="tab-archived" onclick="switchUserTab('archived')"
                        class="btn btn-sm text-muted rounded-0 px-3 py-2">
                    Archived
                </button>
            </div>
        </div>

        <div class="d-flex align-items-center gap-2 w-100-sm">
            <select class="form-select form-select-sm rounded-pill border-0 shadow-sm px-3 flex-grow-1 flex-md-grow-0" style="min-width: 130px;">
                <option selected>All Genders</option>
                <option value="Male">Male</option>
                <option value="Female">Female</option>
                <option value="Other">Other</option>
            </select>

            <div class="search-container shadow-sm flex-grow-1 flex-md-grow-0">
                <input type="text" class="form-control rounded-pill border-0 px-3 py-2" placeholder="Search Costumer">
                <i class="bi bi-search"></i>
            </div>
        </div>
    </div>

    <div id="view-active" class="consumer-grid-wrapper bg-white shadow-sm overflow-hidden rounded-4">
        <div class="table-responsive">
            <table class="table mb-0 align-middle text-center" style="min-width: 900px;">
                <thead>
                    <tr>
                        <th class="border-start">Name</th>
                        <th class="border-start">Number</th>
                        <th class="border-start">Email</th>
                        <th class="border-start">Address</th>
                        <th class="border-start">Gender</th>
                        <th class="border-start">Date Joined</th>
                        <th class="border-start">Status</th>
                        <th class="border-start">Action</th>
                    </tr>
                </thead>
                <tbody>
                    @for (int i = 1; i <= 90; i++)
                    {
                        <tr>
                            <td class="border-start text-secondary">costumerName@(i)</td>
                            <td class="border-start text-secondary">091234567@(i)</td>
                            <td class="border-start text-secondary">user@(i)@@gmail.com</td>
                            <td class="border-start text-secondary">Cebu City</td>
                            <td class="border-start text-secondary">Female</td>
                            <td class="border-start text-secondary">Date@(i)</td>
                            <td class="border-start"><span class="badge bg-success opacity-75">Active</span></td>
                            <td class="border-start">
                                <div class="action-icons">
                                    <i class="bi bi-pencil me-3 text-primary" onclick="openEditModal('costumerName@(i)')" style="cursor: pointer;"></i>
                                    <i class="bi bi-trash text-danger" onclick="openConfirmModal('delete', 'costumerName@(i)')" style="cursor: pointer;"></i>
                                </div>
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>

    <div id="view-archived" class="consumer-grid-wrapper bg-white shadow-sm d-none overflow-hidden rounded-4">
        <div class="table-responsive">
            <table class="table mb-0 align-middle text-center" style="min-width: 900px;">
                <thead class="bg-light">
                    <tr>
                        <th class="border-start">Name</th>
                        <th class="border-start">Number</th>
                        <th class="border-start">Email</th>
                        <th class="border-start">Address</th>
                        <th class="border-start">Gender</th>
                        <th class="border-start">Deletion Date</th>
                        <th class="border-start">Action</th>
                    </tr>
                </thead>
                <tbody>
                    <tr>
                        <td class="border-start text-secondary">DeletedUser_01</td>
                        <td class="border-start text-secondary">0999888777</td>
                        <td class="border-start text-secondary">old_account@@mail.com</td>
                        <td class="border-start text-secondary">Manila</td>
                        <td class="border-start text-secondary">Male</td>
                        <td class="border-start text-danger">Mar 07, 2026</td>
                        <td class="border-start">
                            <button class="btn btn-sm btn-outline-dark rounded-pill py-0 px-3"
                                    onclick="openConfirmModal('restore', 'DeletedUser_01')" style="font-size: 0.7rem;">
                                Restore
                            </button>
                        </td>
                    </tr>
                </tbody>
            </table>
        </div>
    </div>
</div>

<div class="modal fade" id="editModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-dialog-centered">
        <div class="modal-content border-0 shadow rounded-4">
            <div class="modal-header border-0 pt-4 px-4">
                <h5 class="fw-bold">Edit Consumer Details</h5>
                <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
            </div>
            <div class="modal-body px-4">
                <form id="editForm">
                    <div class="mb-3">
                        <label class="small fw-bold text-secondary">Full Name</label>
                        <input type="text" class="form-control rounded-3" id="editName">
                    </div>
                    <div class="row">
                        <div class="col-md-6 mb-3">
                            <label class="small fw-bold text-secondary">Number</label>
                            <input type="text" class="form-control rounded-3" id="editNumber">
                        </div>
                        <div class="col-md-6 mb-3">
                            <label class="small fw-bold text-secondary">Gender</label>
                            <select class="form-select rounded-3" id="editGender">
                                <option value="Male">Male</option>
                                <option value="Female">Female</option>
                            </select>
                        </div>
                    </div>

                    <div class="mb-3">
                        <label class="small fw-bold text-secondary">Email Address</label>
                        <input type="text" class="form-control rounded-3" id="editEmail">
                    </div>

                    <div class="mb-3">
                        <label class="small fw-bold text-secondary">Address</label>
                        <textarea class="form-control rounded-3" id="editAddress" rows="2"></textarea>
                    </div>
                </form>
            </div>
            <div class="modal-footer border-0 pb-4 px-4">
                <button type="button" class="btn btn-light rounded-pill px-4" data-bs-dismiss="modal">Cancel</button>
                <button type="button" class="btn btn-dark rounded-pill px-4" onclick="saveChanges()">Save Changes</button>
            </div>
        </div>
    </div>
</div>

<div class="modal fade" id="confirmModal" tabindex="-1" aria-hidden="true">
    <div class="modal-dialog modal-sm modal-dialog-centered">
        <div class="modal-content border-0 shadow rounded-4 text-center">
            <div class="modal-body p-4">
                <div id="confirmIcon" class="mb-3"></div>
                <h5 class="fw-bold" id="confirmTitle">Are you sure?</h5>
                <p class="small text-secondary" id="confirmMessage">This action cannot be undone.</p>
                <div class="d-flex gap-2 justify-content-center mt-4">
                    <button type="button" class="btn btn-light rounded-pill px-4" data-bs-dismiss="modal">No</button>
                    <button type="button" id="confirmBtn" class="btn rounded-pill px-4 text-white">Yes, Proceed</button>
                </div>
            </div>
        </div>
    </div>
</div>

<div class="toast-container position-fixed bottom-0 end-0 p-3" style="z-index: 1060;">
    <div id="actionToast" class="toast border-0 shadow rounded-4" role="alert" aria-live="assertive" aria-atomic="true">
        <div class="d-flex align-items-center bg-dark text-white rounded-4 p-2">
            <div class="toast-body d-flex align-items-center gap-2">
                <i id="toastIcon" class="bi bi-check-circle-fill text-success fs-5"></i>
                <span id="toastMessage">Action completed successfully!</span>
            </div>
            <button type="button" class="btn-close btn-close-white me-2 m-auto" data-bs-dismiss="toast" aria-label="Close"></button>
        </div>
    </div>
</div>

@section Scripts {
    <script src="~/js/admin_js/users.js" asp-append-version="true"></script>
}